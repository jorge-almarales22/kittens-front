version: '3'

services:
  app:
    container_name: kittens
    # image: kittens
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      
    command: ["npm", "start"]